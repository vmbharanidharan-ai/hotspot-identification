# Downstream design validation (M6)
seed: 42
primary_metric: af2_ipae
higher_is_better: false
tolerance: 0.5
min_improvement_fraction: 0.05
control_groups:
  - random
  - exposed_only
  - central_only
  - predicted
reference_group: predicted
inputs_dir: artifacts/design_inputs
outputs_dir: artifacts/design_outputs
metrics_dir: artifacts/metrics
ranking:
  top_k: 20
  proteinmpnn: false
  af2_multimer: false
  rosetta: false
